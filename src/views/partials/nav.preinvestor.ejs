<nav class="nav">
  <div class="nav-title">Pre-Investor</div>
  <a href="/dashboard" class="nav-link">Welcome</a>
  <a href="/updates" class="nav-link" style="display: flex; align-items: center; gap: 8px;">
    Updates
    <span id="notification-badge" style="display: none; background: #009775; color: white; border-radius: 10px; padding: 2px 6px; font-size: 11px; font-weight: 700;"></span>
  </a>
  <a href="/documents" class="nav-link">Teaser Pack</a>
  <div class="nav-divider"></div>
  <a href="/account" class="nav-link">Account</a>
  <a href="/account/preferences" class="nav-link">Email Preferences</a>
</nav>

<script>
// Fetch notification count on page load
fetch('/api/notifications/count')
  .then(r => r.json())
  .then(data => {
    if (data.hasNew && data.count > 0) {
      const badge = document.getElementById('notification-badge');
      if (badge) {
        badge.textContent = data.count;
        badge.style.display = 'inline-block';
      }
    }
  })
  .catch(err => console.error('Failed to fetch notifications:', err));
</script>
